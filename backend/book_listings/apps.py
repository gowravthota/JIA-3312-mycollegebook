from django.apps import AppConfig


class BookListingsConfig(AppConfig):
    """Simple app config for the BookListings app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "book_listings"
